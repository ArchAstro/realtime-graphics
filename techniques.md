# Realtime graphics techniques

**The browsable version of this guide is the HTML site:**

→ open **[techniques/index.html](./techniques/)** via `./scripts/serve.sh`

That page includes:

- curriculum table with live demo links
- each technique explained with prompt-match phrases
- **source prompts** linked for every technique
- full prompt archive index

The interactive demos are under [`techniques/demos/`](./techniques/demos/).
Raw prompt sources remain in [`prompts/`](./prompts/) (each folder also has a generated `index.html` for the site).
